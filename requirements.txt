# Core Dependencies (Required)
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.20.0
soundfile
numpy
huggingface_hub

# Optimization Dependencies (Optional but Recommended)
# Install these if you want to use 4-bit quantization or Flash Attention
bitsandbytes>=0.41.0
flash-attn>=2.0.0